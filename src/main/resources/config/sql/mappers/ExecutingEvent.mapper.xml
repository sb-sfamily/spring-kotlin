<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ExecutingEvent">

    <insert id="insertExecutingEvent">
        INSERT INTO executing_event
        (title,
         event_serial_number,
         operator_id,
         reg_date,
         from_dt,
         to_dt,
         place_name,
         place_addr)
        VALUES(#{title},
               #{eventSerialNumber},
               #{operatorId},
               #{regDate},
               #{fromDt},
               #{toDt},
               #{placeName},
               #{placeAddr});
    </insert>


    <select id="selectingExcutingEventWithRegDate">
        select regDate from executing_event as exec 
        left join Event as ev on exec.regDate = ev.regDate
        <if test="title != null and title != '' ">
            AND title like CONCAT('%',#{title}, '%')
        </if>
        <choose>
            <when test="statusOfEvent == 'ING'">
                AND current_timestamp() between from_dt and to_dt
        </choose>
    </select>


</mapper>
